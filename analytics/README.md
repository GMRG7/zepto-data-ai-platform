# Module 2 — Analytics Pipeline

Use the Titanic dataset. Follow the assignment's exact requirements: profiling, missingness percentages and threshold-based handling, EDA charts, survival breakdowns, exact six-column correlation matrix, train/test split before preprocessing, three classifiers, imbalance comparison, Random Forest GridSearchCV with OOB, fare regression, and a serialized full pipeline.

Suggested notebooks: `01_eda.ipynb` and `02_modeling.ipynb`. Save a local offline CSV fallback as `titanic.csv`.
